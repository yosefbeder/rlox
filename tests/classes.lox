class Breakfast {
  serve(who) {
    if (3 > 0) {
      print(this.x);
      print("Enjoy your " + this.before() + " " + who);
    }
  }

  before() {
    return "🥪";
  }

  after() {
    return "💩";
  }
}

var instance = Breakfast();
instance.x = "yosef";

instance.before();
instance.serve("yosef");
instance.after();